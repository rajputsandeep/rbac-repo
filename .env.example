# Copy to .env and set values
PORT=8000
NODE_ENV=development
JWT_SECRET=super-secret-change-me
JWT_EXPIRES_IN=1h

# Optional: if you want clients to send an X-Tenant-Id header that must match token. Default true.
ENFORCE_TENANT_HEADER=true

# Security
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Postgres
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=22041992
DB_NAME=postgres


# render 
DATABASE_URL=githuburl
DB_SSL=true